import { ImgLazyLoaderDirective } from './img-lazy-loader.directive';

describe('ImgLazyLoaderDirective', () => {
  it('should create an instance', () => {
    const directive = new ImgLazyLoaderDirective();
    expect(directive).toBeTruthy();
  });
});
